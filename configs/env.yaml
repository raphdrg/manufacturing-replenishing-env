# Environment-level knobs (not part of the scenario distribution).
invalid_action_mode: terminate   # terminate | penalize
max_invalid: 0                   # verifier returns 0 if invalid_count > max_invalid
step_limit_factor: 12            # T_max = step_limit_factor * H + step_limit_offset
step_limit_offset: 40
